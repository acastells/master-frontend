name: CI workflow

on:
  push:
    branches:
      - feature/laboratorio-testing-obligatorio

defaults:
  run:
    working-directory: ./05-testing/laboratorio_1

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install
        run: npm i
      - name: Unit tests
        run: npm test